% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rates_through_time.R
\name{summarize_process_rates_through_time}
\alias{summarize_process_rates_through_time}
\title{Summarize biogeographic process rates through time}
\usage{
summarize_process_rates_through_time(bsm_tables, n_bins = 10L)
}
\arguments{
\item{bsm_tables}{A list of standardized BSM tables containing at least
\code{bsm_events}, and optionally \code{bsm_event_summary}, as returned in a workflow
result's \code{bsm_tables}.}

\item{n_bins}{Number of equal-width time bins between the present and the
oldest sampled event.}
}
\value{
A data frame with one row per model, process, and time bin, including
\code{bin_start}, \code{bin_end}, \code{bin_midpoint}, \code{mean_count}, \code{sd_count},
\code{ci_lower} and \code{ci_upper} (the 2.5\% and 97.5\% percentiles of the per-map
count across stochastic maps), and \code{rate}. Returns an empty table with the
same columns when no timed BSM events are available.
}
\description{
Bin the sampled BioGeoBEARS stochastic mapping (BSM) events by time before
present and summarize, per biogeographic process and time bin, the mean and
standard deviation of the event count per stochastic map, and the mean rate
(events per unit time). This gives a standardized view of when each
biogeographic process occurred that BioGeoBEARS does not produce directly.
}
